{
  "cmr-email": {
    "description": "Only run if there are unread emails in the CMR inbox",
    "mode": "all",
    "checks": [
      {
        "name": "unread-emails-exist",
        "type": "shell",
        "command": "gws gmail users messages list --params '{\"userId\":\"me\",\"q\":\"is:unread in:inbox\",\"maxResults\":1}' 2>/dev/null | grep -c '\"id\"'",
        "expect": "non-zero-output"
      }
    ],
    "timeoutMs": 15000
  }
}
